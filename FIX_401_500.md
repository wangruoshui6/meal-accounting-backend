# 401 和 500 错误修复指南

## 🔍 问题分析

### 401 错误（未授权）
**原因：** JWT token 无效或未传递

### 500 错误（服务器内部错误）
**可能原因：**
1. API Key 配置格式错误 ✅ 已修复
2. API Key 无效或过期
3. 阿里云百炼 API 调用失败
4. 代码异常

## ✅ 已修复的问题

### 1. API Key 配置格式错误

**错误配置：**
```yaml
api-key: ${sk-9a347114b9a54885a5e8008cab3203bd}  # ❌ 错误格式
```

**正确配置：**
```yaml
api-key: ${BAILIAN_API_KEY:sk-9a347114b9a54885a5e8008cab3203bd}  # ✅ 正确格式
```

或者直接配置（仅用于开发测试）：
```yaml
api-key: sk-9a347114b9a54885a5e8008cab3203bd  # ✅ 直接配置
```

## 🔧 解决步骤

### 步骤 1: 修复 API Key 配置

已自动修复 `application.yml` 中的 API Key 配置格式。

**请确认配置：**
```yaml
aliyun:
  bailian:
    api-key: ${BAILIAN_API_KEY:sk-9a347114b9a54885a5e8008cab3203bd}
```

### 步骤 2: 重启后端服务

```bash
# 停止当前运行的后端服务（Ctrl+C）
# 然后重新启动
cd meal-accounting-backend
mvn spring-boot:run
```

### 步骤 3: 检查 401 错误

**401 错误通常是因为：**

1. **用户未登录**
   - 确保已登录并获取了 token
   - 检查浏览器 localStorage 中是否有 `token`

2. **Token 已过期**
   - 重新登录获取新 token
   - 检查 JWT 配置的过期时间

3. **Token 格式错误**
   - 检查请求头格式：`Authorization: Bearer {token}`
   - 确保 token 前有 "Bearer " 前缀

**测试方法：**
```bash
# 1. 先登录获取 token
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"your-username","password":"your-password"}'

# 2. 使用 token 测试聊天接口
curl -X POST http://localhost:8080/api/chat/message \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -H "Content-Type: application/json" \
  -d '{"message":"你好","includeContext":true}'
```

### 步骤 4: 检查 500 错误

**查看后端日志，查找具体错误信息：**

常见错误：

1. **API Key 配置错误**
   ```
   请配置阿里云百炼 API Key
   ```
   **解决：** 检查 `application.yml` 中的 API Key 配置

2. **API 调用失败**
   ```
   API 请求失败: 401 - Invalid API Key
   ```
   **解决：** API Key 无效，检查阿里云控制台

3. **API 响应解析失败**
   ```
   无法解析 API 响应
   ```
   **解决：** 可能是 API 格式变化，查看日志中的实际响应

4. **Redis 连接失败**
   ```
   Unable to connect to Redis
   ```
   **解决：** 确保 Redis 服务已启动

## 🧪 完整测试流程

### 1. 检查后端服务
```bash
# 测试后端是否运行
curl http://localhost:8080/api/chat/test
```

**期望响应：**
```json
{
  "success": true,
  "message": "聊天服务正常运行",
  "timestamp": ...
}
```

### 2. 登录获取 Token
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'
```

**保存返回的 token**

### 3. 测试聊天接口
```bash
curl -X POST http://localhost:8080/api/chat/message \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message":"你好","includeContext":true}'
```

## 📋 检查清单

- [ ] API Key 配置格式正确（已修复）
- [ ] 后端服务已重启
- [ ] Redis 服务已启动
- [ ] 用户已登录并获取 token
- [ ] Token 未过期
- [ ] 请求头包含正确的 Authorization
- [ ] 查看后端日志确认具体错误

## 🐛 常见错误及解决

### 错误 1: "请配置阿里云百炼 API Key"
**原因：** API Key 配置格式错误或未读取到
**解决：** 
1. 检查 `application.yml` 配置
2. 确认配置格式：`api-key: ${BAILIAN_API_KEY:your-key}`
3. 重启后端服务

### 错误 2: "API 请求失败: 401"
**原因：** API Key 无效
**解决：**
1. 检查阿里云控制台的 API Key
2. 确认 API Key 是否正确复制
3. 检查是否有空格或特殊字符

### 错误 3: "用户未登录" (401)
**原因：** JWT token 无效
**解决：**
1. 重新登录获取新 token
2. 检查前端是否正确发送 token
3. 检查 token 是否过期

### 错误 4: "无法解析 API 响应" (500)
**原因：** 阿里云 API 响应格式变化
**解决：**
1. 查看后端日志中的实际响应
2. 可能需要调整 `BailianApiService.java` 中的响应解析逻辑

## 📞 获取帮助

如果问题仍未解决，请提供：
1. 后端完整错误日志
2. 浏览器 Network 标签的请求详情
3. 后端启动日志
4. Redis 连接状态

---

**修复后请重启后端服务并重新测试！**

